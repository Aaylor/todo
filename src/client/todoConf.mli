(** Contains the configuration of the executable. *)

val version : string
(** The current version of the program. *)

val base_folder : string
(** The base folder containing both todo and done folder. *)

val todo_folder : string
(** The folder which contains every todo files. *)

val done_folder : string
(** The folder which contains every done tasks. *)
